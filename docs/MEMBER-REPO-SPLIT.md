# Splitting a member into its own repository

**Scope:** suite-wide. **Status:** the machinery landed 2026-09-21, the day
after the first member (archivext) left; every remaining member is prepared
by it and held prepared by a gate. **Read this before moving a member out,
and read the "when a member leaves" section before deleting its directory** -
that is the step archivext skipped, and thirteen suite gates went red for it.

The suite began as the source of truth for nine sibling extensions and three
apps, with the pre-suite repositories demoted to mirrors. The decision now is
the other way round: this monorepo is the **development** repository, and
each member is to live in a repository of its own. This document is the
procedure and the per-member state. It cites the tools by name; the tools
are the authority, and `python3 tools/member-registry.py` prints the table
this page describes.

## 1. What "ready" means, and the gate that holds it

A member directory is ready to be a repository when it can be pushed as one
and work from its first commit: its README does not 404 on its own links,
its CI runs the gates the suite ran for it, its tools do not crash looking
for a suite that is not there, and a contributor can find out what it
carries from elsewhere and what it cannot check alone. That is five things,
and every one is either generated from a registry or refused by a gate, so
readiness is a property of the tree rather than of the afternoon someone
last checked:

| What | Where it lives | Held by |
|---|---|---|
| The member table: name, title, kind, target repository, native lane, gate siblings | `tools/member-registry.py` | `check-member-standalone.py` refuses a member-shaped directory the table does not list, and a row whose directory is gone |
| The member's own gate list | `<member>/tools/run-gates.sh` (hand-written, with the why-comments that used to live in `build-all.sh`) | `build-all.sh` DELEGATES to it, so the suite and the standalone repository run one list; `check-member-standalone.py` refuses a gate file under `tools/` or `tests/` the script never names |
| The member's own CI | `<member>/.github/workflows/native.yml` (native members) and `gates.yml` (all) | GENERATED by `tools/sync-member-workflows.py` from the root `native-<member>.yml` lanes and the registry; `--check` refuses drift and any other file in that directory |
| The README's "Relationship to the xTalk suite" section | between two HTML-comment sentinels at the end of `<member>/README.md` | GENERATED by `tools/sync-member-readmes.py` from the registry and the carried-copy registries; `--check` refuses drift |
| The kit: `README.md`, `LICENSE`, `CLAUDE.md`, `.gitignore`, `.gitattributes` where binaries are committed, no markdown link that climbs out of the member, no tool that climbs to the suite root except through the sibling helper | the member directory | `tools/check-member-standalone.py`, with `tools/test-member-standalone.py` proving each check fires |

All four tools run in `tools/build-all.sh --gates` (the suite block), so a
push that un-readies a member fails CI here.

## 2. The sibling layout

Some gates reach into sibling members, and this is the one thing a standalone
checkout has to arrange by hand. The rule, implemented by a small `sibling()`
helper in each tool that needs it:

- A sibling member is found at `../<name>` beside the member's checkout,
  under its **member name** (`coinxt`, not `CoinXT`). In the suite tree that
  is simply the neighbouring directory; in a standalone layout it is the
  sibling's repository cloned beside this one under that name.
- `XTALK_SIBLINGS=<dir>` names a directory holding all of them instead;
  `XTALK_SIBLING_<NAME>=<path>` names one (`XTALK_SIBLING_COINXT`,
  `XTALK_SIBLING_HOLDE_EM`).
- A member's own name resolves to its own tree whatever the directory is
  called, because riptide's runner asks for `coinxt` when coinxt is running
  it.
- A sibling that is absent is reported by the gate that needs it, naming the
  path, the repository to clone and the two variables - never a traceback.
  Where a gate used to SKIP a tier without its sibling (nostrxt's tier 2,
  riptide's native signing paths), `XTALK_REQUIRE_SIBLINGS=1` turns that
  skip into a failure; the generated `gates.yml` sets it, for the reason
  `tests/cross-member-test.py` records: in CI a skip and a pass both exit 0.

**Gate dependency is not runtime dependency.** The table lists what a
member's GATES load. What its shipped stacks need, they carry: a demo that
uses a sibling's library holds a verbatim copy between sentinels
(`tools/sync-demo-embeds.py`), so nothing has to be installed or cloned to
paste and run it. The list is written closed by hand - a gate that loads
riptide's runner inherits what that runner loads at import time (nostrxt's
interpreter and oracle), while a gate that loads only coinxt's committed
binary inherits none of coinxt's gate needs - and the registry's docstring
says why no transitive walk over the table would get that right.

| Member | Gate siblings | Why |
|---|---|---|
| nostrxt | coinxt, sodiumxt | tier 2 of `check-script-vectors.py` executes the composed paths against the committed x86_64-linux binaries |
| riptide | nostrxt, coinxt | `lcs-interp.py` and the nostrxt library and oracle are loaded, not copied; the committed coinxt binary signs |
| coinxt | riptide, nostrxt | `check-wallet-boot.py` reuses riptide's boot runner, which loads nostrxt's interpreter and oracle at import |
| nocloud | riptide, nostrxt, coinxt | `check-script-vectors.py` boots the helpers through riptide's runner |
| holde-em | riptide, nostrxt, coinxt | the same runner plus riptide's oracle |
| every other member | none | `bash tools/run-gates.sh` needs nothing but the checkout and Python 3 |

## 3. The target repositories

| Member | Repository | State on 2026-09-21 |
|---|---|---|
| sodiumxt | `SethMorrowSoftware/SodiumXT` | the pre-suite home, a mirror since the fold |
| torrentxt | `SethMorrowSoftware/TorrentXT` | the pre-suite home (it once vendored enetxt/ and datachannelxt/ as subfolders) |
| enetxt | `SethMorrowSoftware/enetxt` | **to be created** - it never had a repository of its own |
| datachannelxt | `SethMorrowSoftware/dataChannelXT` | the pre-suite home |
| box2dxt | `SethMorrowSoftware/Box2Dxt` | the family ancestor's own repository |
| coinxt | `SethMorrowSoftware/CoinXT` | the pre-suite home |
| onionxt | `SethMorrowSoftware/OnionXT` | the pre-suite home |
| nostrxt | `SethMorrowSoftware/NostrXT` | **to be created** - born in the suite |
| riptide | `SethMorrowSoftware/RipTide` | the capstone's pre-suite home |
| nocloud | `SethMorrowSoftware/nocloud` | the pre-suite home |
| holde-em | `SethMorrowSoftware/hold-em` | the pre-suite `hold-em` repository |

The names are in `tools/member-registry.py` and nowhere else; the generated
`gates.yml` files check siblings out from them, and the README sections
point at them. Change the registry and re-run the two generators to rename
one. A member's checkout directory must still be its member name (section 2),
whatever the repository is called.

## 4. The procedure, per member

1. **Create the target repository** if the table says so (enetxt, nostrxt).
   Empty, no README - the export brings everything.
2. **Confirm readiness in the suite:** `python3 tools/check-member-standalone.py`
   and `bash tools/build-all.sh --gates` green on the commit you export.
3. **Export with history.** The suite's history was squashed on 2026-08-23,
   so a member's earlier history is in its pre-suite mirror, not here; what
   this exports is the suite-era history of the member's directory:

   ```sh
   git subtree split -P <member> -b split/<member>
   git push <target-url> split/<member>:main      # or open it as a PR onto the mirror's main
   ```

   (`git filter-repo --subdirectory-filter <member>` on a fresh clone gives
   the same tree with the same commits rewritten to the member root; use it
   when the target must not carry any suite-only path at all.)
4. **In the target, run the member alone.** Clone it as `<member>`, clone
   its gate siblings beside it under their member names (section 2), run
   `bash tools/run-gates.sh`, then push and watch `gates.yml` and, for a
   native member, `native.yml` - both derived from the lanes the suite runs,
   not the pre-suite snapshots that used to sit there.
5. **In the target, retitle what is now history.** The README section says
   the suite is the source of truth "until the split is complete"; the
   member's `CLAUDE.md` still cites `docs/OXT-ENGINE-NOTES.md` and its
   siblings at the suite URL (by design - those documents stay suite-level).
   box2dxt's README badge points at the suite's lane; repoint it at the
   member's own `native.yml` once that has run.
6. **In the suite, retire the member** (section 5). Only then delete the
   directory.

## 5. When a member leaves: the registries that name it

archivext was deleted from this tree on 2026-09-21 with none of its rows
removed, and the fast gate set went red in thirteen places. Every one names
what is missing, so the build points at each site; this is the list so the
next departure removes them in the same change as the directory:

- `tools/member-registry.py` - the row (and `check-member-standalone.py`
  refuses a row whose directory is gone).
- `tools/check-checker-drift.py` - `COPY_SETS` (the checker, docs-style and
  interpreter copies) and `TEMPLATE_SETS`.
- `tools/test-checker.py` - `MEMBERS`.
- `tools/check-ui-kit-drift.py` - `ADOPTERS` (and `EXEMPT`, if the member
  had a written exemption).
- `tools/check-demo-selfcheck-drift.py` - `ADOPTERS` (which
  `check-demo-control-lists.py` reads).
- `tools/check-harness-scaffold-drift.py` - `ADOPTERS`, if the member's
  selftest carried the scaffold.
- `tools/test-stack-size.py` - the expected dimension count and its dated
  comment.
- `tools/check-cross-library-names.py` - the library corpus.
- `start-here.livecodescript` - the launcher's registry rows (held by
  `check-launcher-registry.py`).
- `tools/check-suite-coverage.py` - the coverage row and, if the member's
  library was embedded in the paste, `REQUIRED_EMBEDS`.
- `tools/check-suite-selftest.py` - `PREFIXES` and `ENTRY_POINTS`.
- `tools/build-suite-selftest.py` - its `Member(...)` row and any
  `Layer(...)` row; then `python3 tools/build-suite-selftest.py` to
  regenerate the paste, and `tests/suite-selftest.core.livecodescript`'s
  probe and deep-self-test sections for it.
- `tools/build-preflight.py` - its script-layer probe; then
  `python3 tools/build-preflight.py`.
- `tools/sync-demo-embeds.py` - `REGISTRY` rows for its demos, and any row
  in another member's demo that carried its library (that copy stays in the
  carrier as a snapshot; say so in the carrier's README).
- `tools/build-all.sh` - the member loop.
- The root docs: `README.md`'s member table, release matrix and coverage
  table; `CLAUDE.md`'s tree; `docs/EXTENSIONS-OVERVIEW.md`,
  `docs/OXT-PASS-RUNBOOK.md` and `docs/REMAINING-WORK.md`, which keep their
  dated records under a MOVED OUT note rather than losing them.

Then `bash tools/build-all.sh --gates`. The gates that read the tree by
glob (the harness scaffold, the stack-size scan, the duplicate-declaration
scan, the timer-pin closure) need no edit; the ones above are registries,
and a registry is exactly what a departure has to be told about.

## 6. What a standalone repository cannot check

These run only in the suite, because their question is about more than one
member. A standalone member's `run-gates.sh` does not pretend to ask them:

- **Carried-copy drift.** The demo UI kit, the boot self-check block and the
  harness scaffold are carried verbatim into each stack; their masters and
  drift gates (`tools/ui-kit.livecodescript` and friends) live here. A
  standalone copy can go stale relative to its master; refresh it from the
  suite rather than editing inside the markers.
- **Embed freshness.** A demo carrying a sibling's library holds a snapshot;
  `tools/sync-demo-embeds.py --check` holds it current here and nowhere else.
- **One name, one library** (`tools/check-cross-library-names.py`), the
  cross-member handler-call and typed-boundary gates
  (`check-handler-calls.py`, `check-lcb-call-types.py`), the timer-pin
  closure, the shim scaffold drift, the binary-freshness walk and the suite
  paste's coverage ratchet.
- **The suite paste itself** (`tests/suite-selftest.livecodescript`), which
  folds every member's harness into one stack, and `tests/cross-member-test.py`.

Until the members' repositories are the source of truth, the working answer
is to keep developing here and export (section 4, step 3) after a change
lands, so the cross-member gates run on every push and the standalone
repositories receive verified trees. If the direction reverses - members
develop in their own repositories and the suite pulls them in - the suite
needs a `git subtree pull` per member and the same gate set on the result;
that is an open decision, recorded in `docs/OPEN-DECISIONS.md` when it is
taken.

## 7. Per-member readiness, 2026-09-21

Every member passes `check-member-standalone.py`. What is worth knowing
beyond the green:

| Member | Notes |
|---|---|
| sodiumxt | Nothing beside it needed. Its `native.yml` builds all five platforms; the committed binaries and MANIFEST travel with it. |
| torrentxt | `examples/torrent-quickshare.livecodescript` deliberately embeds nothing (it defines the engine socket handlers itself) and still wants `start using stack "onionxt"` for Tor; the carried onionxt copy in `torrent-dht-channels` is a snapshot after the split. |
| enetxt | Repository to create. `enet-internet-chat` carries torrentxt's helpers as a snapshot. |
| datachannelxt | Nothing beside it needed. |
| box2dxt | README badge points at the suite lane until its own `native.yml` has run. Its Kit is carried into its own examples by its own `sync-embedded-kit.py`, which travels with it. |
| coinxt | Gate siblings riptide and nostrxt. Its full gate list takes hours (the three wallet gates; `run-gates.sh` says why and runs them at once), so `gates.yml` states the six-hour ceiling. |
| onionxt | Nothing beside it needed. Its library is carried into six other stacks (nocloud, coin-wallet, torrent-dht-channels, riptide-social, holde-em, the suite's closing pass); those copies become snapshots after the split. |
| nostrxt | Repository to create. Gate siblings coinxt and sodiumxt (binaries only); its `lcs-interp.py` is the copy riptide, nocloud and holde-em load, so it must stay byte-identical to coinxt's - a rule only the suite's `check-checker-drift.py` enforces. |
| riptide | Gate siblings nostrxt and coinxt. `LICENSE` added 2026-09-21 (it had none). The capstone specs (`RIPTIDE-SOCIAL-SPEC.md`, `RIPTIDE-PROTOCOL.md`) stay suite-level and are cited by URL. |
| nocloud | Gate siblings riptide, nostrxt, coinxt. |
| holde-em | Gate siblings riptide, nostrxt, coinxt. Its README's extension table now links the sibling repositories rather than `../<member>/`. |
