# The site/ and webapp/ assets are vendored media; git's diff/merge behaviour
# on them is pinned here rather than left to its heuristics. Carried from the
# suite root's .gitattributes so it holds in this app's own repository too.
*.png binary
*.jpg binary
*.wav binary
*.mp3 binary
*.zip binary
